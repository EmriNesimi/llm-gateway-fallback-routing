# Test and lint tooling. Kept out of requirements.txt because the
# Dockerfile installs that file straight into the runtime image — pytest,
# ruff, mypy and pip-audit were all shipping to production, where they are
# dead weight and extra attack surface rather than anything that runs.
#
# Installing this installs the runtime deps too, so `pip install -r
# requirements-dev.txt` is the single command for a dev environment.
-r requirements.txt

pytest>=8.2
pytest-asyncio>=0.23
fakeredis>=2.23
lupa>=2.2  # gives fakeredis EVAL/EVALSHA support, needed for our Lua-scripted token bucket
ruff>=0.6
pytest-cov>=7.1.0
mypy>=1.11
pip-audit>=2.7
